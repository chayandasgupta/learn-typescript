let message: string = "Hello, world!"; // Type ennotation using colon (:type)

const numbers: number[] = [1, 2, 3, 4, 5];
let names: string[] = ["John", "Jane", "Bob"];
names.push("Alice");

const flags: boolean[] = [true, false, true];

enum Role {
  ADMIN = "ADMIN",
  USER = "USER",
  GUEST = "GUEST",
}
